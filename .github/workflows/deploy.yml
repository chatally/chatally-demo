name: Deploy to remote server via scp/ssh

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to remote
    runs-on: ubuntu-latest
    # needs: tests // we might want to run some tests before deploying?
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Configure SSH in runner
        run: |
          mkdir -p ~/.ssh/
          chmod 700 ~/.ssh
          cat >~/.ssh/chatally_id_rsa <<END
          ${{ secrets.SSH_KEY }}END
          chmod 600 ~/.ssh/chatally_id_rsa
          cat >~/.ssh/config <<END
          Host REMOTE_SERVER
            HostName ${{ secrets.SSH_HOST }}
            User ${{ secrets.SSH_USER }}
            IdentityFile ~/.ssh/chatally_id_rsa
            StrictHostKeyChecking no
          END

      - name: Write variables to env file
        run: |
          cat >app/.env <<END
          WHATSAPP_CLOUD_GRAPHAPI_PHONE_NUMBER_ID=${{ secrets.WA_PHONE_NUMBER_ID }}
          WHATSAPP_CLOUD_GRAPHAPI_ACCESS_TOKEN=${{ secrets.WA_ACCESS_TOKEN }}
          WHATSAPP_CLOUD_WEBHOOKS_VERIFY_TOKEN=${{ secrets.WA_VERIFY_TOKEN }}
          WHATSAPP_CLOUD_WEBHOOKS_SECRET=${{ secrets.WA_SECRET }}
          END

      - name: Copy sources
        run: |
          scp -r docker-compose.yml Dockerfile restart-server.sh app REMOTE_SERVER:~/src

      - name: Restart server (remotely)
        run: |
          ssh REMOTE_SERVER '~/src/restart-server.sh'
